name: RIBS
description: Remote Interface to Bash Scripts
group:  Shard
env:
  - PATH: "${PATH}:${HOME}/bin:${HOME}/.local/bin/"
  - RIBS_PATH: "/home/nevrax/shard/tools/ribs"
steps:
  - name: run
    dir: /home/nevrax/ribs/server
    command: bash start_server.sh
