FILE(GLOB SRC *.cpp *.h)

ADD_EXECUTABLE(monitor_service ${SRC})

INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/server/src ${LIBXML2_INCLUDE_DIR} ${MYSQL_INCLUDE_DIR} ${ZLIB_INCLUDE_DIR} ${NEL_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(monitor_service	ryzom_gameshare
						${PLATFORM_LINKFLAGS} 
						${LIBXML2_LIBRARIES}
						${ZLIB_LIBRARY}
						${MYSQL_LIBRARIES}
						${NELMISC_LIBRARY} 
						${NELNET_LIBRARY}
						${NELGEORGES_LIBRARY})
IF(WIN32)
  SET_TARGET_PROPERTIES(monitor_service PROPERTIES 
	LINK_FLAGS "/NODEFAULTLIB:libcmt /SUBSYSTEM:WINDOWS"
	PROJECT_LABEL "Services: Monitor Service (MOS)")
ENDIF(WIN32)
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS monitor_service RUNTIME DESTINATION sbin COMPONENT services)


