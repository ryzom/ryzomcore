
// by default, use WIN displayer
WindowStyle = "WIN";

// by default, use localhost to find the naming service
NSHost = "localhost";

NegFiltersDebug    += { "NETL" };
NegFiltersInfo     += { "NETL" };
NegFiltersWarning  += { };
