FILE(GLOB SRC *.cpp *.h)

ADD_EXECUTABLE(build_shadow_skin ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(build_shadow_skin ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS} nel3d)
IF(WIN32)
  SET_TARGET_PROPERTIES(build_shadow_skin PROPERTIES 
    LINK_FLAGS "/NODEFAULTLIB:libcmt"
    PROJECT_LABEL "Tools, 3D: build_shadow_skin")
ENDIF(WIN32)
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS build_shadow_skin RUNTIME DESTINATION bin COMPONENT tools3d)
