FILE(GLOB SRC *.cpp *.h)

ADD_EXECUTABLE(anim_builder ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(anim_builder ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS} nelmisc nel3d)
SET_TARGET_PROPERTIES(anim_builder PROPERTIES PROJECT_LABEL "Tools, 3D: anim_builder")
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS anim_builder RUNTIME DESTINATION bin COMPONENT tools3d)
