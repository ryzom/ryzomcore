FILE(GLOB SRC *.cpp *.h)

ADD_EXECUTABLE(ig_add ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(ig_add ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS} nel3d)
IF(WIN32)
  SET_TARGET_PROPERTIES(ig_add PROPERTIES 
    LINK_FLAGS "/NODEFAULTLIB:libcmt"
    PROJECT_LABEL "Tools, 3D: ig_add")
ENDIF(WIN32)
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS ig_add RUNTIME DESTINATION bin COMPONENT tools3d)
