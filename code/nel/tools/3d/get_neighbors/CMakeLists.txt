FILE(GLOB SRC *.cpp *.h)

ADD_EXECUTABLE(get_neighbors ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(get_neighbors ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS})
IF(WIN32)
  SET_TARGET_PROPERTIES(get_neighbors PROPERTIES 
    LINK_FLAGS "/NODEFAULTLIB:libcmt"
    PROJECT_LABEL "Tools, 3D: get_neighbors")
ENDIF(WIN32)
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS get_neighbors RUNTIME DESTINATION bin COMPONENT tools3d)
