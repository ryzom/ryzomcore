SUBDIRS(misc)

IF(WITH_3D)
  SUBDIRS(3d)
ENDIF(WITH_3D)

IF(WITH_PACS)
  SUBDIRS(pacs)
ENDIF(WITH_PACS)

IF(WITH_LOGIC)
  SUBDIRS(logic)
ENDIF(WITH_LOGIC)

IF(WITH_GEORGES)
  SUBDIRS(georges)
ENDIF(WITH_GEORGES)

IF(WITH_TESTS)
  ADD_SUBDIRECTORY(nel_unit_test)
ENDIF(WITH_TESTS)

